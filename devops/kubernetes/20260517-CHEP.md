# chep
- 서버 여러 노드의 디스크를 하나로 합치며 분산 저장하는 프로그램.
- ROOK를 활용해 설치/관리함.

## 파일 복제
- 서버 n대에 하나의 파일을 복제해 서버가 다운되어도 복구가 가능하다.
- 저장공간 손실이 있을 수 있기 때문에 Erasure Coding으로 데이터를 조각내 분산 저장하기도 한다.

## 저장 구조
```
ex) mysql
mysql pod - PVC - SC - ROOK - Chep - <노드1, 노드2, 노드3 ...>(분산(복제) 저장)
```
